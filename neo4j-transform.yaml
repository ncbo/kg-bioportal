configuration:
  output_directory: output
  checkpoint: false
transform:
  source:
    kg-bioportal:
      input:
        name: "KG-Bioportal"
        format: tsv
        compression: tar.gz
        filename:
          - data/merged/merged-kg.tar.gz
      output:
        format: neo4j
        uri: neo4j://localhost:7687
        username:
        password: